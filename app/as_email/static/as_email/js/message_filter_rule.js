// Vue Component for a MessageFilterRule
//
import { ref } from "vue";

export default {
  props: {
    header: String,
    pattern: String,
    action: String,
    destination: String,
    order: Number,
  },
  setup(props) {
    // access props.header, etc.
  },
  template: ``,
};
